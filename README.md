# SQL Repair Agent

Paste a broken SQL query and what it was meant to do. An agent fixes it by
running it against a read-only database and iterating. A verifier checks the
result. You approve before anything is saved.

🚧 In progress — v0 (skeleton).
