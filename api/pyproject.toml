[project]
name = "sql-repair-agent-api"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = ["fastapi", "uvicorn[standard]"]

[dependency-groups]
dev = ["pytest", "httpx2"]

[tool.pytest.ini_options]
pythonpath = ["."]